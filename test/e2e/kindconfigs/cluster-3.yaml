# Important: due to kind limitations
# (kind node always has resource capacity == host resource capacity) and our planned test
# cases (resource and non-resource properties), modification of this setup might lead to
# significant test failures.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
- role: worker
- role: worker
- role: worker
